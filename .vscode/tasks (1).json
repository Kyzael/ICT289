{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Graphic Compile",
            "type": "shell",
            "command": "gcc -o test.exe -lfreeglut_static -lopengl32 -lwinmm -lgdi32"
        }
    ]
}