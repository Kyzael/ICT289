#include <stdio.h> /*ask the compiler to include library information needed for input/output */
int main ( ) /* the main function is always there */
{
char ch;
printf(" Hello, world!\n"); /* call library function printf to print the string of characters */
printf("31247952");
ch = getchar( ); /* stops the console window from disappearing */
}
